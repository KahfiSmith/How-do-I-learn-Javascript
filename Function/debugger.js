const createFullname = (firstname, middlename, lastname) => {
  debugger;
  const fullname = `${firstname} ${middlename} ${lastname}`;
  return fullname;
};

const name = createFullname("al", "kahfi", "al");
console.log(name)
